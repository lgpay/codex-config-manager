# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)；版本号遵循语义化版本。

## [0.3.0] - 2026-09-17

### 变更
- **全项目术语统一**：一律用「**启用**」，不再用「切换」。界面按钮固定文案「启用配置」，不再拼接预设名；命令行子命令名 `use` 保持不变，只改输出文字
- **「无状态」改成人话**：界面与命令行中一律表述为「**当前配置未记录**」，并在需要处补一句现状说明（例如「正在使用的配置不属于任何预设」），不再要求用户理解内部术语
- 顶栏精简为 **直按钮「新建」「设置」+ 下拉「工具」「帮助」+ 右侧版本号**：
  - 去掉程序名标题与图标（窗口标题栏已有），去掉「配置」下拉
  - 「设置」从工具菜单里提出来成为直按钮，点一下直接进入，不再套一层下拉
  - 工具菜单精简为三项：同步预设 / 历史恢复 / 进程诊断
  - **菜单项文字一律不带省略号**，靠 4px 间距区分「动作」与「菜单」，不加分隔线
- 「当前配置」区改为两行排版：第一行「当前配置：<预设名>」+ 状态标签，第二行「模型 / 供应商 / 应用时间」
- 预设卡片字段名改为中文（模型 / 供应商），不再显示接口地址与密钥变量行；「未同步变化 ≈N 行」改为「有更新」
- 卡片右键菜单六项改为两字短词：启用 / 编辑 / 对比 / 复制 / 另存为 / 删除
- 操作边栏的两个按钮文案固定，ChatGPT 运行状态改为贴边栏底部单独一行并带状态色；「启用配置」下方不再显示任何文字
- 状态条横幅文案重写，每句都写清「发生了什么 + 会怎样 / 怎么办」
- 弹层文案整体对齐：向导标题简化为「新建」，模板名只留名字，字段名与编辑器统一为中文（模型 / 供应商 / 接口地址 / API 密钥）；`本地检查（不联网）` → `检查配置`，`测试模型调用…` → `测试模型…`；`确认切换` → `确认启用`；`配置位置` → `设置`；`运行状态` → `进程诊断`

### 新增
- 界面预览新增场景切换（默认 / 当前配置有更新 / 预设已不存在 / 当前配置未记录 / 空预设 / 读取出错），可逐场景核对文案

### 修复
- **运行状态不实时**：退出 Codex / ChatGPT 后，列表右侧的「启用配置」按钮仍保持置灰、点不动。原因是低频轮询只刷新 ChatGPT 的状态，宿主运行状态（`guard.running`）仅在界面渲染那一刻计算一次，之后不再更新。现在轮询同时刷新两者，退出后按钮会自动恢复可点，「同步预设」等菜单项同步恢复；重新启动宿主后也会重新置灰。另外在窗口重新获得焦点（以及标签页重新可见）时补查一次，覆盖「弹层打开 / 界面忙碌期间轮询被跳过」的窗口期

## [0.2.0] - 2026-09-17

### 界面
- 顶栏新增「配置 / 工具 / 帮助」三个下拉菜单，把原先挤在右侧「操作」栏的低频功能（编辑、另存、复制、删除、同步、差异、历史恢复、进程诊断、演练、配置位置、帮助）全部收进菜单；预设卡片支持右键菜单（切换 / 编辑 / 差异 / 复制 / 另存 / 删除）
- 预设列表右侧新增操作边栏，常驻按钮只剩两个：「切换」与「启动 ChatGPT」
- 运行状态不再单独占位置：不能切换的原因（如「Codex 正在运行」）写在切换按钮提示里，ChatGPT 的「未运行 / 后台驻留 / 已打开」写在启动按钮提示里
- 状态条改为只在有必须显眼的横幅时出现，没有横幅时整条折叠，把纵向空间让给预设列表

### 变更
- 新建配置向导从五种模板精简为两种：**官方服务**（创建完全空白的预设，首次打开 Codex / ChatGPT 时由宿主自动补齐官方默认配置）与**自定义模型**（预设文件只写大模型相关设置：模型 ID、供应商、Base URL、密钥变量名、接口协议）
- 「启动 ChatGPT」按钮与「切换」同处右侧操作边栏，按 ChatGPT 自身状态联动：界面已打开时禁用并说明原因；仅有后台驻留进程时仍可点击（AUMID 激活会把已有实例带到前台）
- 新增低频状态轮询（仅状态变化时重绘），在外部手动开关 ChatGPT 后按钮会自动跟随
- 版本号统一在 `core.VERSION` 维护一处，网络请求的 User-Agent 与界面预览改为动态引用，发版时不再需要多处手改

### 新增
- 界面内新增「启动 ChatGPT」按钮：只读查询系统应用列表定位入口后启动，不猜测安装路径、不读取登录凭据
- 新增可交互界面预览（`tools/make_ui_preview.py` → `outputs/ui-preview.html`），可在浏览器里核对菜单、右键与提示，不触碰真实配置
- 新增界面结构无头探针（`tools/t_gui_menu.py`）与真实配置只读复核（`tools/t_real_readonly.py`）
- CI 核心测试补入启动器与 ChatGPT 联动用例（`src/test_launcher.py`、`src/test_chatgpt.py`），新模块随推送被覆盖

### 修复
- 修正三处测试探针自身缺陷（与产品行为无关）：编辑探针的「字节数持平」断言硬编码 `255` 且新旧 URL 本就不等长；「密钥已设置」断言依赖本机真实用户环境变量；打包 GUI 烟测依赖用户预设库里存在名为 `example` 的预设，用户删除该预设后整段失去覆盖

### 计划
- 干净 Windows 10/11 环境兼容性验证
- 可选代码签名与安装包

## [0.1.0] - 2026-09-17

### 新增
- Codex 配置预设的新建、编辑、复制、删除、切换与历史恢复
- 切换前回收当前配置，避免 Codex 运行期间产生的修改丢失
- TOML 文本级定点改写，保留注释、顺序、多行结构和换行风格
- API Key 写入 Windows 当前用户环境变量，配置文件仅保存变量名
- 自动获取兼容服务的模型列表，并支持 Responses / Chat 最小调用测试
- 首次配置路径探测、自定义 Codex 根目录和预设目录
- 配置模板、未保存提醒、进程保护和脱敏诊断
- 单文件 Windows GUI 与命令行双通道

### 安全
- 默认仅允许 HTTPS；本机回环地址可使用 HTTP
- 网络测试不跟随重定向，限制超时和响应大小
- 密钥不写入 TOML、日志、差异预览或通用任务参数
