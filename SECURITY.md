# 安全策略

## 支持范围

当前维护版本为 `0.1.x`。项目尚处于早期发布阶段，请在升级前备份 Codex 配置。

## 报告安全问题

请不要在公开 Issue 中提交 API Key、配置文件原文、访问令牌或可复现的敏感数据。

优先通过 GitHub 仓库的 **Security → Report a vulnerability**（Private vulnerability reporting / Security Advisories）私下报告。若仓库尚未启用该功能，请先提交一个不含技术细节的普通 Issue，请维护者开启私密报告渠道。

报告建议包含：

- 受影响版本
- 问题类型和影响范围
- 不含真实密钥的最小复现步骤
- 建议修复方向（如有）

## 密钥处理边界

- API Key 写入 Windows 当前用户环境变量，不写入预设 TOML。
- 同一 Windows 用户下运行的其他程序通常可以读取用户环境变量；这不是加密保险箱。
- 网络测试会在用户逐次确认后向指定服务发送密钥。
- 日志、差异预览和诊断输出不得包含密钥值。
